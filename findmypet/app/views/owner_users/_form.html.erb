<%= form_for(@owner_user) do |f| %>
  <% if @owner_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@owner_user.errors.count, "error") %> prohibited this owner_user from being saved:</h2>

      <ul>
      <% @owner_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.text_field :owner_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
